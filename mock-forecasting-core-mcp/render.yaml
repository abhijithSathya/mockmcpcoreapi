services:
  - type: web
    name: forecasting-core-entity-mock-mcp
    runtime: node
    plan: free
    rootDir: mock-forecasting-core-mcp
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
